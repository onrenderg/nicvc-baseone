<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NICVC.ViewSavedAlertsPage" 
              xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls" 
              ios:Page.UseSafeArea="true">
    <NavigationPage.TitleView  >
        <StackLayout x:Name="Custom_Navigation" Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" >

            <Image Source="ic_launcher.png"  HeightRequest="40"  VerticalOptions="CenterAndExpand"/>
            <StackLayout  Orientation="Vertical" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Spacing="0">

                <Label x:Name="Lbl_Header" Text="NIC Video Conferencing" Style="{StaticResource NavigationLabelColor}" FontSize="20" FontAttributes="Bold" HorizontalTextAlignment="Center"
                      VerticalTextAlignment="Center" />
               
            </StackLayout>
        </StackLayout>
    </NavigationPage.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="ic_home.png" Clicked="ToolbarItem_Clicked" />
    </ContentPage.ToolbarItems>
    <Grid>
        <StackLayout>
            <Label x:Name="lbl_user_header1" Padding="3"  FontSize="18" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Margin="2,2,2,0" FontAttributes="Bold" TextColor="White" BackgroundColor="Navy" Text="Today  VC"/>

            <Label x:Name="lbl_user_header" Padding="3" FontSize="18" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"  Margin="2" FontAttributes="Bold"
                   TextColor="White" BackgroundColor="Navy"  Text="Today VC"/>
            <Grid Margin="5">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>


                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40*"/>
                    <ColumnDefinition Width="60*"/>
                </Grid.ColumnDefinitions>


                <Label x:Name="lbl_selectdate" Text="Select Date"  TextColor="Navy" VerticalTextAlignment="Center" HorizontalTextAlignment="Start"  FontAttributes="Bold" Grid.Row="0" Grid.Column="0"/>
                <Picker x:Name="picker_selectdate" Title="Select Date" TextColor="Maroon"  HorizontalOptions="FillAndExpand"  Grid.Row="0" Grid.Column="1" SelectedIndexChanged="picker_sort_SelectedIndexChanged" ios:Picker.UpdateMode="WhenFinished" />


            </Grid>

            <ListView x:Name="listView_alertvc" HasUnevenRows="True" SeparatorColor="#126b8a" ItemTapped="listView_alertvc_ItemTapped" SelectionMode="None">
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <ViewCell>
                            <Frame Margin="5"  HasShadow="False" CornerRadius="20" BackgroundColor="White" BorderColor="Navy">
                                <Grid HorizontalOptions="FillAndExpand">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*"/>
                                        <ColumnDefinition Width="4*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Text="{Binding labelname}" TextColor="Maroon"  FontSize="Default" Grid.Column="0"  FontAttributes="Bold"/>
                                    <Label Text="{Binding NoOfVc}" TextColor="Navy" HorizontalTextAlignment="Center"  FontSize="Default" Grid.Column="1"  FontAttributes="Bold"  />

                                    <Image Source="ic_vcforward" HeightRequest="35" Grid.Column="2"></Image>
                                </Grid>
                            </Frame>

                        </ViewCell>

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!--<ListView x:Name="listView_alertvc" HasUnevenRows="True" SeparatorColor="#126b8a" ItemTapped="listView_alertvc_ItemTapped" SelectionMode="None">
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <ViewCell >
                            <Frame Margin="5"  HasShadow="False" CornerRadius="20" BackgroundColor="White" BorderColor="Navy">
                                <Grid HorizontalOptions="FillAndExpand">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*"/>
                                        <ColumnDefinition Width="4*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackLayout Orientation="Vertical" Grid.Column="0">

                                        <Label Text="VC Date" TextColor="Navy"  HorizontalTextAlignment="Center" FontSize="Default" FontAttributes="Bold" VerticalTextAlignment="Center" />
                                        <Label Text="{Binding DateofVCtoshowonly}" TextColor="Maroon"  FontSize="Default" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"  FontAttributes="Bold"/>
                                    </StackLayout>
                                    <StackLayout Orientation="Vertical" Grid.Column="1">

                                        <Label Text="No. Of Vc's" TextColor="Navy"  HorizontalTextAlignment="Center" FontSize="Default" FontAttributes="Bold" VerticalTextAlignment="Center" />
                                        <Label Text="{Binding NoOfVc}" TextColor="Maroon" HorizontalTextAlignment="Center"  FontSize="Default" FontAttributes="Bold"  VerticalTextAlignment="Center"  />
                                    </StackLayout>


                                    <Image Source="ic_vcforward" HeightRequest="35" Grid.Column="2"  ></Image>
                                </Grid>
                            </Frame>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->



        </StackLayout>
        <ContentView x:Name="Loading_activity" BackgroundColor="#C0808080" Padding="20, 20" IsVisible="false" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Frame BackgroundColor="White" HorizontalOptions="FillAndExpand">
                    <StackLayout Orientation="Horizontal" Margin="6" HorizontalOptions="FillAndExpand">
                        <ActivityIndicator IsVisible="true" IsRunning="true" Margin="5"/>
                        <Label x:Name="Lbl_PleaseWait" Text="Please Wait ..." TextColor="Black" FontAttributes="Bold" HorizontalOptions="FillAndExpand" WidthRequest="600" VerticalTextAlignment="Center"/>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ContentView>
    </Grid>



</ContentPage>